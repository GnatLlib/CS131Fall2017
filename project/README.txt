TO RUN SERVERS:

python3 server.py [SERVER_NAME]

Valid server names are 
Alford, Ball, Hamilton, Holiday, Welsh

Each server generates a .log file names after itself
EG. Alford server generates an alford.log file. 

testclient.py contains a few tests that expects all servers to be running. 